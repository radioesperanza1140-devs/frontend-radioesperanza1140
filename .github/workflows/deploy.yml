# name: Deploy Angular to VPS

# on:
#   push:
#     branches:
#       - main

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#     - name: Checkout code
#       uses: actions/checkout@v3

#     - name: Setup Node.js
#       uses: actions/setup-node@v3
#       with:
#         node-version: 20

#     - name: Install dependencies
#       run: npm ci

#     - name: Build Angular app
#       run: npm run build --prod

#     - name: Deploy to VPS
#       env:
#         SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
#       run: |
#         echo "$SSH_PRIVATE_KEY" > key.pem
#         chmod 600 key.pem
#         ssh -i key.pem -o StrictHostKeyChecking=no root@radioesperanza1140.com "
#           rm -rf /var/www/html/*
#           mkdir -p /var/www/html
#         "
#         scp -i key.pem -o StrictHostKeyChecking=no -r dist/root@radioesperanza1140.com:/var/www/html/
#         rm key.pem
